<div class="navbar-filters navbar-troveko-sticky" style="position: sticky; top: 0;">

  <div class="hidden-xs hidden-sm">
        <%= link_to 'Grid', experiences_path, remote: true, method: :get, class: 'btn-primary btn-xl' %>
        <%= link_to 'Map', experiences_path(map: true), remote: true, method: :get, class: 'btn-primary btn-xl' %>
      </div>

  <div class="navbar-troveko-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" style="color: black;" data-toggle="dropdown"></i>
      <ul class="dropdown-menu">
        <li><%= link_to 'Grid', experiences_path, remote: true, method: :get %></li>
        <li><%= link_to 'Map', experiences_path(map: true), remote: true, method: :get %></li>
      </ul>
    </div>
  </div>

    <div class="floatright">
      <% if current_user&.ngo? %>
      <%= link_to 'Create New Experience', new_experience_path, class: 'btn btn-secondary' %>
      <% end %>
    </div>

  </div>
</div>






